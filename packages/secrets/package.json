{
  "name": "@aigovops/secrets",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.mjs",
  "description": "The secrets broker — one SecretsProvider interface, tiered backends. Agents never hold raw credentials; the gate issues scoped, expiring grants."
}
