# M22 — hosted billing env, prepared. Values are 1Password references (op://), resolved at runtime by
# the broker — never commit real keys. The human's one-time act is storing the live key in 1Password.
AIGOVOPS_HOSTED=1
AIGOVOPS_STRIPE_KEY=op://AiGovOps/stripe-live-key/credential
AIGOVOPS_STRIPE_PRICE_TEAM=op://AiGovOps/stripe-price-team/credential
AIGOVOPS_STRIPE_PRICE_ENTERPRISE=op://AiGovOps/stripe-price-enterprise/credential
